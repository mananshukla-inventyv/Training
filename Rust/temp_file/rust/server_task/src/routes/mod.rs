// use axum::Router;

// use crate::{health_check::health_check, helper_func::crud_ops};

// pub async fn create_routes() -> Router {
//     Router::new().merge(health_check()).merge(crud_ops())
// }
